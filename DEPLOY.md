# Birusk — چی درست شد و چطور دیپلوی کنی

## معماری فعلی (که این فیکس‌ها بر همین اساس انجام شده)

- **Railway (birusk-master)** = مرکز: پنل مدیریت، دیتابیس SQLite، و همچنین یک نود
  مستقیم به نوع `railway` (کلاینت‌ها می‌تونن مستقیم به همین سرور هم وصل بشن).
- **Cloudflare Worker (birusk-node/cf-worker)** = نودِ لبه (edge). بدون KV،
  فقط هر ۶۰ ثانیه یک بار با یک GET/POST متنی ساده با پنل سینک میشه:
  - `GET /api/sync` → لیست UUID های فعال رو می‌گیره (تو حافظه‌ی Worker نگه می‌داره)
  - `POST /api/usage` → مصرف جمع‌شده رو گزارش میده
  این دقیقاً همون چیزیه که خواسته بودی: بدون KV و رفت‌وآمد سبک، پس محدودیت روزانه‌ی
  Worker (درخواست/CPU) خیلی دیرتر پر میشه چون حجم اصلی ترافیک (تونل WebSocket) از
  حد رایگان Workers جدا حساب نمیشه به شکل درخواست‌های API.

## چی باگ بود و چی فیکس شد

1. **`go.mod` ناقص بود** — `main.go` پکیج `gorilla/websocket` رو ایمپورت می‌کرد ولی
   توی `go.mod` نبود. اضافه شد.
2. **ترنسپورت‌های الکی** — پنل اجازه‌ی ساخت نود با `xhttp`/`splithttp`/`grpc`/`tcp`
   و امنیت `reality` رو میداد، در حالی که نه `main.go` نه `worker.js` هیچ‌کدوم از
   این‌ها رو واقعاً پیاده نکرده بودن (و `tcp`/`grpc`/`reality` اصلاً روی Cloudflare
   Worker امکان‌پذیر نیستن چون Worker فقط HTTP/WebSocket می‌گیره، نه TCP خام).
   نتیجه: کاربر یه ساب‌سکریپشن می‌گرفت که وصل نمیشد. این گزینه‌ها از UI حذف شدن و
   `handleSubscription` هم فقط برای `ws`+`tls` (تنها چیزی که واقعاً کار می‌کنه)
   کانفیگ می‌سازه.
3. **باگ iOS/Safari در Worker** — بدون فلگ `no_web_socket_compression`، کلاینت‌های
   مبتنی بر Safari (Shadowrocket, Streisand, FoXray روی iOS) معمولاً اصلاً وصل
   نمیشن. به `wrangler.toml` اضافه شد.
4. **`wrangler.toml` و `package.json` اصلاً وجود نداشتن** توی `cf-worker/` — بدون
   این فایل‌ها اصلاً نمیشه Worker رو دیپلوی کرد. ساخته شدن.
5. **سخت‌گیری روی پکت‌های خراب/کوتاه** — هم توی `main.go` هم `worker.js`، اگه یه
   پکت اول ناقص/بد فرمت می‌رسید (مثلاً به‌خاطر باگ کلاینت)، احتمال index-out-of-range
   بود. حالا قبل از خوندن هر بخش، طولش چک میشه و به‌جای کرش/خطای بی‌صدا، فقط
   کانکشن بسته میشه.

## دیپلوی پنل (Railway)

1. ریپازیتوری رو به Railway وصل کن (یا از همین Dockerfile استفاده کن).
2. Railway خودش `PORT` رو ست می‌کنه، نیازی به تنظیم دستی نیست.
3. بعد از بالا اومدن، آدرس عمومی‌ای که Railway میده (مثلاً
   `https://your-app.up.railway.app`) رو یادداشت کن — این همون `MASTER_URL` نودته.
4. وارد پنل شو → یه نود از نوع **Cloudflare** بساز → یه توکن (`token`) براش
   ساخته میشه، اون رو کپی کن (لازمش داری برای مرحله‌ی بعد).

## دیپلوی نود (Cloudflare Worker)

```bash
cd birusk-node/cf-worker
npm install

# لاگین به اکانت Cloudflare (فقط بار اول)
npx wrangler login
```

توی `wrangler.toml`، مقدار `MASTER_URL` رو با آدرس واقعی Railway‌ت جایگزین کن، بعد:

```bash
# توکنی که پنل برای این نود ساخت رو اینجا وارد کن (پرامپت می‌پرسه)
npx wrangler secret put NODE_TOKEN

# دیپلوی نهایی
npx wrangler deploy
```

بعد از دیپلوی، آدرسی مثل `https://birusk-node.YOUR-SUBDOMAIN.workers.dev` می‌گیری.
این آدرس رو به‌عنوان **Address** همون نودی که توی پنل ساختی وارد کن (Transport: `WS`,
Security: `TLS`, Port: `443`).

## تست سریع

```bash
curl https://birusk-node.YOUR-SUBDOMAIN.workers.dev
# باید ببینی: Birusk Edge Node Active ⚡
```

اگه این جواب رو گرفتی، یعنی Worker بالاست. بعدش یه یوزر توی پنل بساز، لینک
ساب‌سکریپشن (`/sub?id=...`) رو بگیر و توی یه کلاینت (v2rayNG, Streisand, ...) وارد
کن.

## قدم بعدی (اختیاری، اگه واقعاً REALITY/gRPC/XHTTP لازم داری)

راه درستِ پشتیبانی کامل از این ترنسپورت‌ها، پیاده‌سازی دستی‌شون نیست (خیلی پیچیده
و مستعد باگه)، بلکه دقیقاً همون کاری‌ه که پروژه‌های معروف مثل Marzban/3x-ui
می‌کنن: باینری واقعی **xray-core** رو (شبیه به همون کاری که همین پروژه الان برای
`mtg_core` توی MTProto انجام میده — دانلود و اجرا به‌عنوان subprocess) روی همون
سرویس Railway اجرا کنی و پنل فقط کانفیگ JSON یوزرها رو بهش بده و از API آماری‌اش
مصرف بخونه. اگه خواستی روی این جهت کار کنیم، بگو تا شروع کنم — ولی پروژه‌ی
جدا و بزرگ‌تریه نسبت به فیکس‌های الان.
